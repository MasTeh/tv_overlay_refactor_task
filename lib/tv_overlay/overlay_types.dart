enum OverlayVisibility { visible, hidden }

enum PlaybackPhase { loading, playing, paused, failed }

enum OverlayFocusTarget {
  surface,
  back,
  playPause,
  audioSubtitles,
  settings,
  progress,
  skipIntro,
}

enum TvDirection { up, down, left, right }

enum TvRemoteCommand { up, down, left, right, select, back }

enum OverlayDialog { audioSubtitles, settings }
